{
  "name": "eternity-id",
  "version": "1.0.0",
  "private": true,
  "description": "ZK-Degree & Course Verifier — IITG Ethereum Privacy Hackathon",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm --filter contracts test",
    "deploy:sepolia": "pnpm --filter contracts deploy:sepolia"
  },
  "engines": {
    "node": "22.x",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "ethers": "^6.17.0"
  }
}
